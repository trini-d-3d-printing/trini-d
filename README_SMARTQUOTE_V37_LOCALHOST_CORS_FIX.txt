Smart Quote v37

Fixes GitHub Pages to localhost backend connection by trying both 127.0.0.1 and localhost.
Use Backend Stage 6, which adds Private Network Access/CORS headers.
If the backend is not connected, the page will not show browser fallback values as final quote.
