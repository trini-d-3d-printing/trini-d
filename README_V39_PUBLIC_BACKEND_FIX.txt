V39 fixes the architecture problem: GitHub Pages customers cannot access the business owner's localhost.

Admin > Export / Settings now stores a Public Smart Quote Backend URL in Firebase.
The public Smart Quote reads that HTTPS URL in realtime and uses localhost only for same-PC testing.

Website ZIP contains no backend, no .env, no .venv, and no PrusaSlicer files.
